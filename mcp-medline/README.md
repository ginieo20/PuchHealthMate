### Medline MCP Server

- Dev: `pnpm i && pnpm dev` (or `npm i && npm run dev`)
- Deploy: `pnpm deploy`
- Endpoint: SSE at default Worker entry

Tool: `medline_search`
- input: `{ "query": "asthma treatment" }`
- returns: XML from MedlinePlus search API